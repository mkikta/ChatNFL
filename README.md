# ChatNFL

To run the project, first ensure that you have ElasticSearch, Docker, npm, and Ollama installed.

1. Run the docker daemon.
2. Run elasticsearch locally with
     ```bash
     curl -fsSL https://elastic.co/start-local | sh.
     ```
4. Run ollama with
